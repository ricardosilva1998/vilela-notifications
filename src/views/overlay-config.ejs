<%- include('header', { title: 'Overlay Config', streamer: streamer }) %>

<style>
  /* Pill toggle */
  .pill-toggle { position: relative; display: inline-block; width: 44px; height: 24px; flex-shrink: 0; }
  .pill-toggle input { opacity: 0; width: 0; height: 0; }
  .pill-toggle .slider { position: absolute; cursor: pointer; inset: 0; background: var(--bg-elevated); border-radius: 24px; border: 1px solid var(--border); transition: 0.3s; }
  .pill-toggle .slider:before { content: ''; position: absolute; height: 18px; width: 18px; left: 2px; bottom: 2px; background: #fff; border-radius: 50%; transition: 0.3s; }
  .pill-toggle input:checked + .slider { background: var(--accent); border-color: var(--accent); }
  .pill-toggle input:checked + .slider:before { transform: translateX(20px); }

  /* Alert card */
  .alert-card { background: var(--bg-surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 20px; margin-bottom: 14px; transition: border-color 0.2s; }
  .alert-card:hover { border-color: rgba(145,70,255,0.3); }
  .alert-card-header { display: flex; align-items: center; gap: 12px; margin-bottom: 18px; }
  .alert-card-title { font-size: 15px; font-weight: 700; color: var(--text-primary); }
  .alert-card-desc { font-size: 12px; color: var(--text-muted); margin-top: 2px; }
  .alert-card-body { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 16px; }
  @media (max-width: 600px) { .alert-card-body { grid-template-columns: 1fr; } }
  .alert-field label { display: block; font-size: 11px; font-weight: 600; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 6px; font-family: var(--font-display); }
  .alert-card-footer { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 10px; padding-top: 14px; border-top: 1px solid var(--border); }
  .sound-row { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
  .sound-badge { font-size: 11px; padding: 2px 8px; border-radius: 4px; background: var(--bg-elevated); color: var(--text-muted); font-weight: 500; }
  .sound-badge.custom { background: rgba(145,71,255,0.15); color: #9146ff; }

  /* Volume slider */
  .vol-row { display: flex; align-items: center; gap: 10px; }
  .vol-row input[type=range] { flex: 1; accent-color: var(--accent); cursor: pointer; }
  .vol-val { font-family: var(--font-display); font-weight: 600; font-size: 13px; min-width: 36px; text-align: right; }

  /* Preview modal */
  .preview-modal { display: none; position: fixed; inset: 0; z-index: 9999; }
  .preview-modal.open { display: flex; align-items: center; justify-content: center; }
  .preview-backdrop { position: absolute; inset: 0; background: rgba(0,0,0,0.85); backdrop-filter: blur(4px); }
  .preview-content { position: relative; z-index: 1; display: flex; flex-direction: column; align-items: center; gap: 16px; padding: 24px; max-width: 900px; width: 95vw; }
  .preview-screen { width: 100%; aspect-ratio: 16/9; background: #0a0a0a; border-radius: 8px; border: 1px solid rgba(255,255,255,0.1); overflow: hidden; position: relative; box-shadow: 0 0 60px rgba(0,0,0,0.6); }
  .preview-screen iframe { width: 100%; height: 100%; border: none; display: block; }
  .preview-screen-label { position: absolute; top: 10px; left: 12px; font-size: 11px; color: rgba(255,255,255,0.3); font-family: var(--font-display); pointer-events: none; }
  .preview-actions { display: flex; gap: 10px; align-items: center; }
  .preview-status { font-size: 12px; color: rgba(255,255,255,0.5); font-family: var(--font-display); }

  /* Trim modal */
  .trim-modal { display: none; position: fixed; inset: 0; z-index: 10000; }
  .trim-modal.open { display: flex; align-items: center; justify-content: center; }
  .trim-backdrop { position: absolute; inset: 0; background: rgba(0,0,0,0.75); }
  .trim-content { position: relative; z-index: 1; background: var(--bg-surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 28px; width: 480px; max-width: 95vw; }
  .trim-content h4 { font-size: 16px; font-weight: 700; margin-bottom: 6px; }
  .trim-content p { font-size: 13px; color: var(--text-secondary); margin-bottom: 20px; }
  .trim-field { margin-bottom: 16px; }
  .trim-field label { display: flex; justify-content: space-between; font-size: 12px; font-weight: 600; color: var(--text-secondary); font-family: var(--font-display); margin-bottom: 6px; text-transform: uppercase; letter-spacing: 0.05em; }
  .trim-field input[type=range] { width: 100%; accent-color: var(--accent); cursor: pointer; }
  .trim-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 4px; }
  #trim-audio-player { width: 100%; margin-bottom: 16px; border-radius: 4px; }
</style>

<a href="/dashboard" style="color: var(--text-secondary); font-size: 13px; font-family: var(--font-display); font-weight: 500; display: inline-flex; align-items: center; gap: 6px;">
  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
  Back to Dashboard
</a>
<h2 style="margin: 16px 0 8px; font-size: 24px; font-weight: 700;" class="animate-in">Overlay Configuration</h2>
<p style="color: var(--text-secondary); font-size: 14px; margin-bottom: 24px;">Set up your stream overlay to show live alerts for follows, subscriptions, bits, and donations.</p>

<% if (needsReauth) { %>
  <div class="alert alert-warning">
    <strong>Broadcaster re-authentication required.</strong>
    Your Twitch broadcaster token is missing required scopes (<code>moderator:read:followers</code>, <code>bits:read</code>).
    <a href="/auth/broadcaster" style="color: var(--warning); text-decoration: underline; margin-left: 6px;">Re-authenticate now &rarr;</a>
  </div>
<% } %>

<!-- Overlay URL -->
<div class="card animate-in">
  <h3>
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="12" cy="12" r="3"/><path d="M12 3v2M12 19v2M3 12h2M19 12h2"/></svg>
    OBS Overlay URL
  </h3>
  <% if (overlayUrl) { %>
    <p style="color: var(--text-secondary); font-size: 14px; margin-bottom: 12px;">
      Add this as a Browser Source in OBS. Recommended size: <strong>1920 &times; 1080</strong>.
    </p>
    <div style="display: flex; gap: 8px; align-items: center;">
      <input type="text" id="overlay-url-input" value="<%= overlayUrl %>" readonly
        style="flex: 1; padding: 10px 14px; background: var(--bg-base); border: 1px solid var(--border);
               border-radius: var(--radius-sm); color: var(--text-primary); font-family: var(--font-body);
               font-size: 13px; outline: none;">
      <button onclick="copyOverlayUrl()" class="btn btn-secondary" id="copy-btn" style="flex-shrink: 0;">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
        Copy URL
      </button>
    </div>
  <% } else { %>
    <p style="color: var(--text-secondary); font-size: 14px; margin-bottom: 16px;">
      Generate a unique overlay URL to use in OBS as a Browser Source.
    </p>
    <form method="POST" action="/dashboard/overlay/generate-token" style="margin: 0;">
      <button type="submit" class="btn btn-primary">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
        Generate Overlay URL
      </button>
    </form>
  <% } %>
</div>

<!-- Main Settings Form -->
<form method="POST" action="/dashboard/overlay">

  <!-- Overlay Status -->
  <div class="card animate-in animate-in-delay-1">
    <h3>
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 8 12 12 14 14"/></svg>
      Overlay Status
    </h3>
    <div class="toggle">
      <input type="checkbox" id="overlay_enabled" name="overlay_enabled" value="1" <%= streamer.overlay_enabled ? 'checked' : '' %>>
      <label for="overlay_enabled">Enable overlay (connect EventSub + StreamElements)</label>
    </div>
    <p style="color: var(--text-muted); font-size: 13px;">When enabled, the overlay will receive live events from Twitch and StreamElements.</p>
  </div>

  <!-- Alerts Section -->
  <div class="animate-in animate-in-delay-2">
    <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px;">
      <div>
        <h3 style="margin: 0 0 4px; font-size: 17px; display: flex; align-items: center; gap: 8px;">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 20V10M12 20V4M6 20v-6"/></svg>
          Alerts
        </h3>
        <p style="color: var(--text-secondary); font-size: 13px; margin: 0;">Configure each event alert individually.</p>
      </div>
      <!-- Master Volume -->
      <div style="display: flex; align-items: center; gap: 10px; flex-shrink: 0;">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/><path d="M19.07 4.93a10 10 0 0 1 0 14.14M15.54 8.46a5 5 0 0 1 0 7.07"/></svg>
        <span style="font-size: 12px; color: var(--text-secondary); font-family: var(--font-display); font-weight: 600; white-space: nowrap;">Master Volume</span>
        <div class="vol-row" style="width: 130px;">
          <input type="range" name="overlay_volume" id="overlay_volume"
            min="0" max="1" step="0.05" value="<%= streamer.overlay_volume != null ? streamer.overlay_volume : 0.8 %>"
            oninput="document.getElementById('volume-val').textContent = Math.round(this.value * 100) + '%'">
          <span class="vol-val" id="volume-val"><%= Math.round((streamer.overlay_volume != null ? streamer.overlay_volume : 0.8) * 100) %>%</span>
        </div>
      </div>
    </div>

    <!-- Follow -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="overlay_follow_enabled" value="1" <%= streamer.overlay_follow_enabled !== 0 ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(145,70,255,0.15); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
        </div>
        <div>
          <div class="alert-card-title">&#127937; Follow</div>
          <div class="alert-card-desc">Show an alert when someone follows</div>
        </div>
      </div>
      <div class="alert-card-body">
        <div class="alert-field">
          <label>Duration (seconds)</label>
          <input type="number" name="overlay_follow_duration" value="<%= streamer.overlay_follow_duration || 5 %>" min="1" max="30"
            style="width: 90px; padding: 7px 10px; background: var(--bg-base); border: 1px solid var(--border);
                   border-radius: var(--radius-sm); color: var(--text-primary); font-family: var(--font-body);
                   font-size: 14px; outline: none; text-align: center;">
        </div>
        <div class="alert-field" style="opacity:0; pointer-events:none;"></div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-follow">Default</span>
          <button type="button" onclick="previewSound('follow')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('follow')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('follow')" class="btn btn-secondary" id="remove-btn-follow" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('follow')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>

    <!-- Subscription -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="overlay_sub_enabled" value="1" <%= streamer.overlay_sub_enabled !== 0 ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(145,70,255,0.15); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 2H3v16h5v4l4-4h5l4-4V2zM11 11V7M16 11V7"/></svg>
        </div>
        <div>
          <div class="alert-card-title">&#127942; Subscription</div>
          <div class="alert-card-desc">Show an alert for new subs and resubs</div>
        </div>
      </div>
      <div class="alert-card-body">
        <div class="alert-field">
          <label>Duration (seconds)</label>
          <input type="number" name="overlay_sub_duration" value="<%= streamer.overlay_sub_duration || 7 %>" min="1" max="30"
            style="width: 90px; padding: 7px 10px; background: var(--bg-base); border: 1px solid var(--border);
                   border-radius: var(--radius-sm); color: var(--text-primary); font-family: var(--font-body);
                   font-size: 14px; outline: none; text-align: center;">
        </div>
        <div class="alert-field" style="opacity:0; pointer-events:none;"></div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-subscription">Default</span>
          <button type="button" onclick="previewSound('subscription')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('subscription')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('subscription')" class="btn btn-secondary" id="remove-btn-subscription" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('subscription')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>

    <!-- Bits -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="overlay_bits_enabled" value="1" <%= streamer.overlay_bits_enabled !== 0 ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(145,70,255,0.15); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
        </div>
        <div>
          <div class="alert-card-title">&#9889; Bits / Cheers</div>
          <div class="alert-card-desc">Show an alert when someone cheers bits</div>
        </div>
      </div>
      <div class="alert-card-body">
        <div class="alert-field">
          <label>Duration (seconds)</label>
          <input type="number" name="overlay_bits_duration" value="<%= streamer.overlay_bits_duration || 6 %>" min="1" max="30"
            style="width: 90px; padding: 7px 10px; background: var(--bg-base); border: 1px solid var(--border);
                   border-radius: var(--radius-sm); color: var(--text-primary); font-family: var(--font-body);
                   font-size: 14px; outline: none; text-align: center;">
        </div>
        <div class="alert-field" style="opacity:0; pointer-events:none;"></div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-bits">Default</span>
          <button type="button" onclick="previewSound('bits')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('bits')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('bits')" class="btn btn-secondary" id="remove-btn-bits" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('bits')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>

    <!-- Donation -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="overlay_donation_enabled" value="1" <%= streamer.overlay_donation_enabled !== 0 ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(62,207,142,0.12); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--success)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
        </div>
        <div>
          <div class="alert-card-title">&#128176; Donation</div>
          <div class="alert-card-desc">Show an alert for StreamElements donations</div>
        </div>
      </div>
      <div class="alert-card-body">
        <div class="alert-field">
          <label>Duration (seconds)</label>
          <input type="number" name="overlay_donation_duration" value="<%= streamer.overlay_donation_duration || 6 %>" min="1" max="30"
            style="width: 90px; padding: 7px 10px; background: var(--bg-base); border: 1px solid var(--border);
                   border-radius: var(--radius-sm); color: var(--text-primary); font-family: var(--font-body);
                   font-size: 14px; outline: none; text-align: center;">
        </div>
        <div class="alert-field" style="opacity:0; pointer-events:none;"></div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-donation">Default</span>
          <button type="button" onclick="previewSound('donation')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('donation')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('donation')" class="btn btn-secondary" id="remove-btn-donation" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('donation')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>

    <!-- Raid -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="overlay_raid_enabled" value="1" disabled>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(255,140,0,0.12); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <span style="font-size: 18px; line-height: 1;">&#127950;</span>
        </div>
        <div>
          <div class="alert-card-title">&#127950; Raid</div>
          <div class="alert-card-desc">Show an alert when another streamer raids your channel</div>
        </div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-raid">Default</span>
          <button type="button" onclick="previewSound('raid')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('raid')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('raid')" class="btn btn-secondary" id="remove-btn-raid" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('raid')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>

    <!-- YT Super Chat -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="yt_overlay_superchat_enabled" value="1" <%= (streamer.yt_overlay_superchat_enabled !== 0) ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(255,0,0,0.10); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <span style="font-size: 18px; line-height: 1;">&#128172;</span>
        </div>
        <div>
          <div class="alert-card-title">&#128172; YT Super Chat</div>
          <div class="alert-card-desc">Show an alert for YouTube Super Chats</div>
        </div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-yt_superchat">Default</span>
          <button type="button" onclick="previewSound('yt_superchat')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('yt_superchat')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('yt_superchat')" class="btn btn-secondary" id="remove-btn-yt_superchat" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('yt_superchat')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>

    <!-- YT Member -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="yt_overlay_member_enabled" value="1" <%= (streamer.yt_overlay_member_enabled !== 0) ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(255,200,0,0.10); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <span style="font-size: 18px; line-height: 1;">&#11088;</span>
        </div>
        <div>
          <div class="alert-card-title">&#11088; YT Member</div>
          <div class="alert-card-desc">Show an alert for new YouTube Members</div>
        </div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-yt_member">Default</span>
          <button type="button" onclick="previewSound('yt_member')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('yt_member')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('yt_member')" class="btn btn-secondary" id="remove-btn-yt_member" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('yt_member')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>

    <!-- YT Gift Member -->
    <div class="alert-card">
      <div class="alert-card-header">
        <label class="pill-toggle">
          <input type="checkbox" name="yt_overlay_giftmember_enabled" value="1" <%= (streamer.yt_overlay_giftmember_enabled !== 0) ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
        <div style="width: 36px; height: 36px; border-radius: 8px; background: rgba(145,70,255,0.10); display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
          <span style="font-size: 18px; line-height: 1;">&#127873;</span>
        </div>
        <div>
          <div class="alert-card-title">&#127873; YT Gift Member</div>
          <div class="alert-card-desc">Show an alert when memberships are gifted</div>
        </div>
      </div>
      <div class="alert-card-footer">
        <div class="sound-row">
          <span style="font-size: 12px; color: var(--text-secondary); font-weight: 600; font-family: var(--font-display);">Sound:</span>
          <span class="sound-badge" id="sound-status-yt_giftmember">Default</span>
          <button type="button" onclick="previewSound('yt_giftmember')" class="btn btn-secondary" style="font-size: 12px; padding: 5px 10px;">&#9654; Preview</button>
          <button type="button" onclick="openTrimModal('yt_giftmember')" class="btn btn-primary" style="font-size: 12px; padding: 5px 10px;">Upload</button>
          <button type="button" onclick="removeSound('yt_giftmember')" class="btn btn-secondary" id="remove-btn-yt_giftmember" style="font-size: 12px; padding: 5px 10px; color: var(--danger); display: none;">Remove</button>
        </div>
        <% if (overlayUrl) { %>
        <button type="button" onclick="openPreview('yt_giftmember')" class="btn btn-primary" style="font-size: 12px; padding: 6px 14px; gap: 6px;">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="5 3 19 12 5 21 5 3"/></svg>
          Preview Alert
        </button>
        <% } %>
      </div>
    </div>
  </div>

  <!-- StreamElements JWT -->
  <div class="card animate-in animate-in-delay-3">
    <h3>
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
      StreamElements Integration
    </h3>
    <p style="color: var(--text-secondary); font-size: 14px; margin-bottom: 16px;">
      Connect your StreamElements account to receive donation alerts. Find your JWT token in
      <strong>StreamElements Dashboard &rarr; Account &rarr; Channels &rarr; Show Token</strong>.
    </p>
    <div class="form-group" style="margin-bottom: 0;">
      <label for="streamelements_jwt">StreamElements JWT Token</label>
      <input type="password" name="streamelements_jwt" id="streamelements_jwt"
        placeholder="eyJhbGciOi..."
        value="<%= streamer.streamelements_jwt || '' %>"
        autocomplete="off">
    </div>
    <% if (streamer.streamelements_jwt) { %>
      <p style="color: var(--success); font-size: 13px; margin-top: 8px; display: flex; align-items: center; gap: 6px;">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        JWT token is saved. Leave blank to remove it.
      </p>
    <% } %>
  </div>

  <!-- Save Button -->
  <div style="display: flex; justify-content: flex-end; margin-bottom: 24px;">
    <button type="submit" class="btn btn-primary" style="padding: 12px 32px; font-size: 14px;">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
      Save Settings
    </button>
  </div>

</form>

<!-- Preview Alert Modal -->
<div class="preview-modal" id="preview-modal">
  <div class="preview-backdrop" onclick="closePreview()"></div>
  <div class="preview-content">
    <div class="preview-screen" id="preview-screen">
      <div class="preview-screen-label">Stream Preview</div>
      <iframe id="preview-iframe" src="" allowtransparency="true"></iframe>
    </div>
    <div class="preview-actions">
      <span class="preview-status" id="preview-status">Firing test event...</span>
      <button type="button" onclick="closePreview()" class="btn btn-secondary">Close</button>
    </div>
  </div>
</div>

<!-- Sound Trim Modal -->
<div class="trim-modal" id="trim-modal">
  <div class="trim-backdrop" onclick="closeTrimModal()"></div>
  <div class="trim-content">
    <h4>Upload Sound</h4>
    <p>Optionally trim the audio before uploading. Use the sliders to set start and end points.</p>
    <audio id="trim-audio-player" controls></audio>
    <div class="trim-field">
      <label>
        <span>Start</span>
        <span id="trim-start-val">0.00s</span>
      </label>
      <input type="range" id="trim-start" min="0" max="100" step="0.1" value="0"
        oninput="onTrimSlider('start')">
    </div>
    <div class="trim-field">
      <label>
        <span>End</span>
        <span id="trim-end-val">0.00s</span>
      </label>
      <input type="range" id="trim-end" min="0" max="100" step="0.1" value="100"
        oninput="onTrimSlider('end')">
    </div>
    <div class="trim-actions">
      <button type="button" class="btn btn-secondary" onclick="closeTrimModal()">Cancel</button>
      <button type="button" class="btn btn-secondary" id="trim-upload-full-btn" onclick="uploadFull()">Upload Full</button>
      <button type="button" class="btn btn-primary" id="trim-apply-btn" onclick="applyTrimAndUpload()">Trim &amp; Upload</button>
    </div>
  </div>
</div>

<script>
  // ─── Audio synth (default sound previews) ───────────────────────────────────
  const audioCtx = new (window.AudioContext || window.webkitAudioContext)();

  function createNoise(duration, vol) {
    const buffer = audioCtx.createBuffer(1, audioCtx.sampleRate * duration, audioCtx.sampleRate);
    const data = buffer.getChannelData(0);
    for (let i = 0; i < data.length; i++) data[i] = (Math.random() * 2 - 1);
    const source = audioCtx.createBufferSource();
    source.buffer = buffer;
    const gain = audioCtx.createGain();
    gain.gain.value = vol;
    source.connect(gain);
    return { source, gain };
  }

  function engineRev(startFreq, endFreq, duration, vol) {
    const t = audioCtx.currentTime;
    const osc1 = audioCtx.createOscillator();
    osc1.type = 'sawtooth';
    osc1.frequency.setValueAtTime(startFreq, t);
    osc1.frequency.exponentialRampToValueAtTime(endFreq, t + duration * 0.7);
    osc1.frequency.exponentialRampToValueAtTime(endFreq * 0.8, t + duration);
    const osc2 = audioCtx.createOscillator();
    osc2.type = 'sawtooth';
    osc2.frequency.setValueAtTime(startFreq / 2, t);
    osc2.frequency.exponentialRampToValueAtTime(endFreq / 2, t + duration * 0.7);
    const distortion = audioCtx.createWaveShaper();
    const curve = new Float32Array(256);
    for (let i = 0; i < 256; i++) { const x = (i * 2) / 256 - 1; curve[i] = (Math.PI + 3.4) * x / (Math.PI + 3.4 * Math.abs(x)); }
    distortion.curve = curve;
    const gain = audioCtx.createGain();
    gain.gain.setValueAtTime(vol, t);
    gain.gain.setValueAtTime(vol, t + duration * 0.8);
    gain.gain.exponentialRampToValueAtTime(0.001, t + duration);
    osc1.connect(distortion); osc2.connect(distortion);
    distortion.connect(gain); gain.connect(audioCtx.destination);
    osc1.start(t); osc1.stop(t + duration);
    osc2.start(t); osc2.stop(t + duration);
  }

  function turboBlowoff(vol) {
    const t = audioCtx.currentTime;
    const spool = audioCtx.createOscillator();
    spool.type = 'sine';
    spool.frequency.setValueAtTime(2000, t);
    spool.frequency.exponentialRampToValueAtTime(6000, t + 0.3);
    const spoolGain = audioCtx.createGain();
    spoolGain.gain.setValueAtTime(vol * 0.3, t);
    spoolGain.gain.exponentialRampToValueAtTime(0.001, t + 0.35);
    spool.connect(spoolGain); spoolGain.connect(audioCtx.destination);
    spool.start(t); spool.stop(t + 0.35);
    const { source: noise, gain: noiseGain } = createNoise(0.25, vol * 0.4);
    const filter = audioCtx.createBiquadFilter();
    filter.type = 'bandpass';
    filter.frequency.setValueAtTime(4000, t + 0.3);
    filter.frequency.exponentialRampToValueAtTime(800, t + 0.55);
    filter.Q.value = 2;
    noiseGain.disconnect(); noise.connect(noiseGain); noiseGain.connect(filter);
    const noiseEnv = audioCtx.createGain();
    noiseEnv.gain.setValueAtTime(vol * 0.5, t + 0.3);
    noiseEnv.gain.exponentialRampToValueAtTime(0.001, t + 0.55);
    filter.connect(noiseEnv); noiseEnv.connect(audioCtx.destination);
    noise.start(t + 0.3); noise.stop(t + 0.55);
  }

  function tireScreech(vol) {
    const t = audioCtx.currentTime;
    const { source: noise, gain: noiseGain } = createNoise(0.3, vol * 0.3);
    const filter = audioCtx.createBiquadFilter();
    filter.type = 'highpass';
    filter.frequency.setValueAtTime(3000, t);
    filter.frequency.exponentialRampToValueAtTime(6000, t + 0.15);
    filter.frequency.exponentialRampToValueAtTime(2000, t + 0.3);
    filter.Q.value = 8;
    noiseGain.disconnect(); noise.connect(noiseGain); noiseGain.connect(filter);
    const env = audioCtx.createGain();
    env.gain.setValueAtTime(vol * 0.35, t);
    env.gain.exponentialRampToValueAtTime(0.001, t + 0.3);
    filter.connect(env); env.connect(audioCtx.destination);
    noise.start(t); noise.stop(t + 0.3);
  }

  const defaultSounds = {
    follow: () => { tireScreech(0.3); setTimeout(() => engineRev(80, 200, 0.4, 0.2), 200); },
    subscription: () => { engineRev(60, 180, 0.6, 0.15); turboBlowoff(0.25); },
    bits: () => { engineRev(100, 400, 0.15, 0.2); setTimeout(() => engineRev(200, 600, 0.15, 0.25), 120); setTimeout(() => engineRev(300, 800, 0.2, 0.3), 240); },
    donation: () => { engineRev(40, 60, 0.3, 0.15); setTimeout(() => engineRev(60, 300, 0.5, 0.25), 250); },
    raid: () => { engineRev(50, 150, 0.6, 0.12); setTimeout(() => engineRev(60, 180, 0.5, 0.15), 100); setTimeout(() => engineRev(70, 200, 0.5, 0.18), 200); setTimeout(() => tireScreech(0.2), 500); },
    yt_superchat: () => { engineRev(80, 250, 0.5, 0.2); setTimeout(() => turboBlowoff(0.2), 300); },
    yt_member: () => { engineRev(50, 150, 0.6, 0.15); },
    yt_giftmember: () => { engineRev(80, 300, 0.3, 0.2); setTimeout(() => engineRev(100, 400, 0.3, 0.25), 250); },
  };

  // ─── Sound status ────────────────────────────────────────────────────────────
  let customSounds = {};

  async function loadSoundStatus() {
    const res = await fetch('/dashboard/overlay/sounds/status');
    customSounds = await res.json();
    Object.keys(customSounds).forEach(type => {
      const statusEl = document.getElementById('sound-status-' + type);
      const removeBtn = document.getElementById('remove-btn-' + type);
      if (customSounds[type]) {
        if (statusEl) { statusEl.textContent = 'Custom'; statusEl.className = 'sound-badge custom'; }
        if (removeBtn) removeBtn.style.display = '';
      } else {
        if (statusEl) { statusEl.textContent = 'Default'; statusEl.className = 'sound-badge'; }
        if (removeBtn) removeBtn.style.display = 'none';
      }
    });
  }

  function previewSound(type) {
    if (audioCtx.state === 'suspended') audioCtx.resume();
    if (customSounds[type]) {
      const audio = new Audio('/overlay/sounds/' + type + '.mp3?t=' + Date.now());
      audio.volume = parseFloat(document.getElementById('overlay_volume').value) || 0.8;
      audio.play().catch(() => {});
    } else if (defaultSounds[type]) {
      defaultSounds[type]();
    }
  }

  async function removeSound(type) {
    if (!confirm('Remove custom sound and revert to default?')) return;
    await fetch('/dashboard/overlay/sounds/' + type + '/delete', { method: 'POST' });
    loadSoundStatus();
  }

  loadSoundStatus();

  // ─── Copy overlay URL ────────────────────────────────────────────────────────
  function copyOverlayUrl() {
    const input = document.getElementById('overlay-url-input');
    if (!input) return;
    navigator.clipboard.writeText(input.value).then(() => {
      const btn = document.getElementById('copy-btn');
      const orig = btn.innerHTML;
      btn.innerHTML = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg> Copied!';
      btn.style.color = 'var(--success)';
      btn.style.borderColor = 'var(--success)';
      setTimeout(() => { btn.innerHTML = orig; btn.style.color = ''; btn.style.borderColor = ''; }, 2000);
    }).catch(() => { input.select(); document.execCommand('copy'); });
  }

  // ─── Preview alert modal ─────────────────────────────────────────────────────
  let previewAutoClose = null;

  function openPreview(type) {
    const modal = document.getElementById('preview-modal');
    const iframe = document.getElementById('preview-iframe');
    const statusEl = document.getElementById('preview-status');
    const overlayUrlBase = document.getElementById('overlay-url-input') ? document.getElementById('overlay-url-input').value : '';

    if (!overlayUrlBase) return;

    // Clear any previous auto-close
    if (previewAutoClose) { clearTimeout(previewAutoClose); previewAutoClose = null; }

    statusEl.textContent = 'Firing test event...';
    iframe.src = overlayUrlBase;
    modal.classList.add('open');

    // Fire the test event after iframe has a moment to load and connect SSE
    setTimeout(() => {
      fetch('/dashboard/overlay/test/' + type, { method: 'POST' })
        .then(r => r.json())
        .then(data => {
          if (data.ok) {
            statusEl.textContent = 'Alert fired — watch the preview screen.';
          } else {
            statusEl.textContent = 'Error: ' + (data.error || 'Unknown error');
          }
        })
        .catch(err => { statusEl.textContent = 'Request failed: ' + err.message; });
    }, 1500);
  }

  function closePreview() {
    const modal = document.getElementById('preview-modal');
    const iframe = document.getElementById('preview-iframe');
    modal.classList.remove('open');
    iframe.src = '';
    if (previewAutoClose) { clearTimeout(previewAutoClose); previewAutoClose = null; }
  }

  // Close on Escape key
  document.addEventListener('keydown', e => {
    if (e.key === 'Escape') {
      closePreview();
      closeTrimModal();
    }
  });

  // ─── Sound trim modal ────────────────────────────────────────────────────────
  let trimEventType = null;
  let trimAudioBuffer = null;
  let trimFile = null;
  let trimDuration = 0;

  function openTrimModal(type) {
    trimEventType = type;
    trimAudioBuffer = null;
    trimFile = null;

    // Open file picker first
    const input = document.createElement('input');
    input.type = 'file';
    input.accept = 'audio/*,.mp3,.wav,.ogg,.m4a';
    input.onchange = async () => {
      const file = input.files[0];
      if (!file) return;
      trimFile = file;

      // Load into audio element for playback
      const url = URL.createObjectURL(file);
      const player = document.getElementById('trim-audio-player');
      player.src = url;

      // Decode for Web Audio
      const arrayBuf = await file.arrayBuffer();
      try {
        trimAudioBuffer = await audioCtx.decodeAudioData(arrayBuf);
        trimDuration = trimAudioBuffer.duration;
      } catch (e) {
        // If decode fails (e.g. format not supported) still let user upload full
        trimDuration = 0;
        trimAudioBuffer = null;
      }

      // Set slider max to duration
      const startSlider = document.getElementById('trim-start');
      const endSlider = document.getElementById('trim-end');
      startSlider.max = trimDuration > 0 ? trimDuration : 100;
      startSlider.value = 0;
      endSlider.max = trimDuration > 0 ? trimDuration : 100;
      endSlider.value = trimDuration > 0 ? trimDuration : 100;
      document.getElementById('trim-start-val').textContent = '0.00s';
      document.getElementById('trim-end-val').textContent = trimDuration > 0 ? trimDuration.toFixed(2) + 's' : '—';

      document.getElementById('trim-apply-btn').disabled = !trimAudioBuffer;
      document.getElementById('trim-modal').classList.add('open');
    };
    input.click();
  }

  function onTrimSlider(which) {
    const startSlider = document.getElementById('trim-start');
    const endSlider = document.getElementById('trim-end');
    let startVal = parseFloat(startSlider.value);
    let endVal = parseFloat(endSlider.value);

    // Clamp: start cannot exceed end
    if (which === 'start' && startVal >= endVal) {
      startVal = endVal - 0.1;
      startSlider.value = startVal;
    }
    if (which === 'end' && endVal <= startVal) {
      endVal = startVal + 0.1;
      endSlider.value = endVal;
    }

    document.getElementById('trim-start-val').textContent = startVal.toFixed(2) + 's';
    document.getElementById('trim-end-val').textContent = endVal.toFixed(2) + 's';
  }

  function closeTrimModal() {
    document.getElementById('trim-modal').classList.remove('open');
    const player = document.getElementById('trim-audio-player');
    player.pause();
    player.src = '';
    trimEventType = null;
    trimAudioBuffer = null;
    trimFile = null;
  }

  async function uploadFull() {
    if (!trimFile || !trimEventType) return;
    const data = await trimFile.arrayBuffer();
    const res = await fetch('/dashboard/overlay/sounds/' + trimEventType, {
      method: 'POST',
      headers: { 'Content-Type': 'audio/mpeg' },
      body: data,
    });
    const result = await res.json();
    if (result.ok) { loadSoundStatus(); closeTrimModal(); }
    else alert('Upload failed: ' + (result.error || 'Unknown error'));
  }

  async function applyTrimAndUpload() {
    if (!trimAudioBuffer || !trimEventType) return;
    const startVal = parseFloat(document.getElementById('trim-start').value);
    const endVal = parseFloat(document.getElementById('trim-end').value);

    const wavBlob = audioBufferToWav(trimAudioBuffer, startVal, endVal);
    const arrayBuf = await wavBlob.arrayBuffer();

    const res = await fetch('/dashboard/overlay/sounds/' + trimEventType, {
      method: 'POST',
      headers: { 'Content-Type': 'audio/mpeg' },
      body: arrayBuf,
    });
    const result = await res.json();
    if (result.ok) { loadSoundStatus(); closeTrimModal(); }
    else alert('Upload failed: ' + (result.error || 'Unknown error'));
  }

  function writeString(view, offset, str) {
    for (let i = 0; i < str.length; i++) view.setUint8(offset + i, str.charCodeAt(i));
  }

  function audioBufferToWav(buffer, start, end) {
    const sampleRate = buffer.sampleRate;
    const numChannels = buffer.numberOfChannels;
    const startSample = Math.floor(start * sampleRate);
    const endSample = Math.floor(end * sampleRate);
    const length = Math.max(0, endSample - startSample);

    const wavBuffer = new ArrayBuffer(44 + length * numChannels * 2);
    const view = new DataView(wavBuffer);

    writeString(view, 0, 'RIFF');
    view.setUint32(4, 36 + length * numChannels * 2, true);
    writeString(view, 8, 'WAVE');
    writeString(view, 12, 'fmt ');
    view.setUint32(16, 16, true);
    view.setUint16(20, 1, true); // PCM
    view.setUint16(22, numChannels, true);
    view.setUint32(24, sampleRate, true);
    view.setUint32(28, sampleRate * numChannels * 2, true);
    view.setUint16(32, numChannels * 2, true);
    view.setUint16(34, 16, true);
    writeString(view, 36, 'data');
    view.setUint32(40, length * numChannels * 2, true);

    let offset = 44;
    for (let i = 0; i < length; i++) {
      for (let ch = 0; ch < numChannels; ch++) {
        const channelData = buffer.getChannelData(ch);
        const s = Math.max(-1, Math.min(1, channelData[startSample + i]));
        view.setInt16(offset, s < 0 ? s * 0x8000 : s * 0x7FFF, true);
        offset += 2;
      }
    }
    return new Blob([wavBuffer], { type: 'audio/wav' });
  }
</script>

<%- include('footer') %>
